[project]
name = "beatsabersonginstaller"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "tkinterdnd2>=0.4.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
